# cv-cloud-aws
Cloud Resume Challenge en AWS Academy: currículum web estático desplegado en AWS Amplify, con dominio propio y HTTPS, backend serverless para contador de visitas (API Gateway, AWS Lambda y DynamoDB), infraestructura como código con Terraform y pipelines de CI/CD.
